DROP TABLE IF EXISTS `sessions`;

CREATE TABLE `sessions` (
  `session_id` VARCHAR(127) NOT NULL AUTO_INCREMENT,
  `last_activity` INT(10) NOT NULL,
  `data` TEXT NOT NULL,
  PRIMARY KEY(`session_id`)
)
TYPE=InnoDB;